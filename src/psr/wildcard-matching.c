/**
 * Given a string `s` and a pattern `p`, implement wildcard matching.
 * The matching function returns `true` iff `s` and `p` entirely matches.
 * The pattern supports `?` and `*` where
 *   `?` matches any single character
 *   `*` matches any sequence of characters, including empty sequence
 *
 * leetcode #44 hard
 * https://leetcode.com/problems/wildcard-matching/
 */
